interface:
  display_name: "Arabic CMS"
  short_description: "Repo-specific patterns and workflows for Arabic-CMS"
  default_prompt: "Use the Arabic-CMS repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true